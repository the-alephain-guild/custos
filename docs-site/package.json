{
  "name": "custos-docs-site",
  "version": "0.1.0",
  "private": true,
  "description": "Documentation site for custos.alephain.com — the non-custodial execution runner",
  "scripts": {
    "docusaurus": "docusaurus",
    "start": "docusaurus start",
    "start:zh": "docusaurus start --locale zh-Hans",
    "build": "docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids",
    "typecheck": "tsc",
    "check:disclosure": "node scripts/check-disclosure.mjs",
    "test:disclosure": "node scripts/test-check-disclosure.mjs",
    "verify": "npm run test:disclosure && npm run check:disclosure && npm run build && npm run typecheck"
  },
  "dependencies": {
    "@docusaurus/core": "^3.6.0",
    "@docusaurus/preset-classic": "^3.6.0",
    "@mdx-js/react": "^3.0.0",
    "clsx": "^2.1.0",
    "prism-react-renderer": "^2.4.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "^3.6.0",
    "@docusaurus/tsconfig": "^3.6.0",
    "@docusaurus/types": "^3.6.0",
    "typescript": "~5.5.0"
  },
  "browserslist": {
    "production": [">0.5%", "not dead", "not op_mini all"],
    "development": ["last 3 chrome version", "last 3 firefox version", "last 5 safari version"]
  },
  "engines": {
    "node": ">=20.0"
  }
}
